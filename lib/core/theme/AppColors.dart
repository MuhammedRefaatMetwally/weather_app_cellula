import 'dart:ui';

class AppColors {
  AppColors._();

  // Background for Scaffold (sky blue, gentle background)
  static const Color scaffoldBackgroundColor = Color(0xffecf0f1);

  // Primary color for AppBar and important elements (a soft blue)
  static const Color primaryColor = Color(0xff3498db);

  // Secondary color for complementary elements (light gray for subtle contrasts)
  static const Color secondaryColor = Color(0xffbdc3c7);

  // Accent color for interactive elements like buttons (vibrant yellow)
  static const Color accentColor = Color(0xfff1c40f);

  // Text field background (soft white/gray for input fields)
  static const Color textFieldBackgroundColor = Color(0xffe0e0e0);

  // Hint text color in input fields
  static const Color hintColor = Color(0xff7f8c8d);

  // Font color for general text (dark gray for readability)
  static const Color fontColor = Color(0xff2c3e50);
}